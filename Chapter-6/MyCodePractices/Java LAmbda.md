### জাভা-এ ল্যামডা এক্সপ্রেশন (Lambda Expressions): প্রেডিকেট, কম্প্যারেটর, কনজিউমার, সাপ্লায়ার এবং ফাঙ্কশন ল্যামডা

হ্যালো! আপনি ল্যামডা এক্সপ্রেশনের বিভিন্ন ধরন সম্পর্কে জানতে চেয়েছেন, বিশেষ করে `Predicate`, `Comparator`, `Consumer`, `Supplier` এবং `Function` ল্যামডা। আমি এগুলোকে সহজ বাংলায় ব্যাখ্যা করব, ধাপে ধাপে। ল্যামডা হলো জাভা ৮-এর একটা ফিচার যা কোডকে শর্ট এবং রিডেবল করে, বিশেষ করে ফাঙ্কশনাল ইন্টারফেস (যেমন একটা মেথডের ইন্টারফেস) এর সাথে। *Core Java* (Volume I, 13th Edition, Chapter 6, p. 274) বলে, ল্যামডা অ্যানোনিমাস ক্লাসের বদলে ব্যবহার করুন কোড সিম্পল করার জন্য। *Effective Java* (Item 42) বলে, ল্যামডা ফাঙ্কশনাল প্রোগ্রামিং-এ সাহায্য করে।

প্রথমে ল্যামডার বেসিক সিনট্যাক্স মনে করুন: `(প্যারামিটার) -> { বডি }`। এখন চলুন প্রত্যেকটার থিয়রি, উদাহরণ এবং গুরুত্ব দেখি। আমি সহজ কোড উদাহরণ দেব যাতে আপনি প্র্যাকটিস করতে পারেন।

#### ১. প্রেডিকেট ল্যামডা (Predicate Lambda): টেস্ট করে ট্রু/ফল্স রিটার্ন
**থিয়রি:** `Predicate<T>` হলো একটা ফাঙ্কশনাল ইন্টারফেস যাতে `test(T t)` মেথড আছে, যা ইনপুট নিয়ে `boolean` রিটার্ন করে। এটা "প্রেডিকেট" মানে শর্ত চেক করার জন্য ব্যবহার হয়। *Core Java* বলে, এটা স্ট্রিম API-তে ফিল্টারিং-এ দরকারি (যেমন লিস্ট থেকে শর্তমতো আইটেম বাছাই)।

**সহজ উদাহরণ:** ধরুন, একটা লিস্ট থেকে বয়স ১৮-এর বেশি লোক ফিল্টার করব।
```java
import java.util.function.Predicate;
import java.util.Arrays;
import java.util.List;

Predicate<Integer> adultCheck = age -> age >= 18; // ল্যামডা
List<Integer> ages = Arrays.asList(15, 20, 17, 25);
ages.stream().filter(adultCheck).forEach(System.out::println); // আউটপুট: 20, 25
```
**কেন গুরুত্বপূর্ণ?** এটা কন্ডিশনাল লজিককে শর্ট করে। অ্যান্ড্রয়েড অ্যাপে ইউজার লিস্ট ফিল্টার করতে ব্যবহার হয়।

#### ২. কম্প্যারেটর ল্যামডা (Comparator Lambda): সর্টিং-এর জন্য কম্পেয়ার
**থিয়রি:** `Comparator<T>` ইন্টারফেসে `compare(T o1, T o2)` মেথড আছে, যা দুটো অবজেক্ট কম্পেয়ার করে ইন্টিজার রিটার্ন করে (নেগেটিভ=প্রথম ছোট, পজিটিভ=দ্বিতীয় ছোট)। ল্যামডা দিয়ে এটা সহজে তৈরি করা যায়। *Core Java* (p. 258) বলে, এটা মাল্টিপল সর্ট অর্ডার সাপোর্ট করে, যেমন স্কোর বা ডেট অনুসারে।

**সহজ উদাহরণ:** সং নাম অনুসারে সর্ট করব।
```java
import java.util.Comparator;
import java.util.Arrays;

Comparator<String> byLength = (s1, s2) -> s1.length() - s2.length(); // ল্যামডা
String[] songs = {"Galactic Jam", "Boogie", "Quantum Tune"};
Arrays.sort(songs, byLength);
System.out.println(Arrays.toString(songs)); // আউটপুট: [Boogie, Jam, Galactic Tune] (শর্ট থেকে লং)
```
**কেন গুরুত্বপূর্ণ?** গেম লিডারবোর্ড বা মিউজিক প্লেলিস্ট সর্ট করতে দরকারি। *Effective Java* (Item 14) বলে, ল্যামডা দিয়ে কাস্টম কম্প্যারেটর তৈরি করুন।

#### ৩. কনজিউমার ল্যামডা (Consumer Lambda): ইনপুট নিয়ে কিছু করে, কিছু রিটার্ন করে না
**থিয়রি:** `Consumer<T>` ইন্টারফেসে `accept(T t)` মেথড আছে, যা ইনপুট নেয় কিন্তু `void` রিটার্ন করে। এটা "কনজিউম" মানে ব্যবহার করে ফেলার জন্য। *Core Java* বলে, স্ট্রিম API-তে ফরইচ লুপের বদলে এটা ব্যবহার হয় (যেমন প্রিন্ট করা)।

**সহজ উদাহরণ:** লিস্টের প্রত্যেক আইটেম প্রিন্ট করব।
```java
import java.util.function.Consumer;
import java.util.Arrays;
import java.util.List;

Consumer<String> printer = str -> System.out.println("Playing: " + str); // ল্যামডা
List<String> playlist = Arrays.asList("Binary Boogie", "Cosmic Groove");
playlist.forEach(printer); // আউটপুট: Playing: Binary Boogie \n Playing: Cosmic Groove
```
**কেন গুরুত্বপূর্ণ?** ইভেন্ট হ্যান্ডলিং বা UI আপডেটে সাহায্য করে। অ্যান্ড্রয়েডে বাটন ক্লিকে এটা ল্যামডা দিয়ে করা যায়।

#### ৪. সাপ্লায়ার ল্যামডা (Supplier Lambda): কোনো ইনপুট ছাড়াই আউটপুট দেয়
**থিয়রি:** `Supplier<T>` ইন্টারফেসে `get()` মেথড আছে, যা কোনো প্যারামিটার ছাড়াই `T` রিটার্ন করে। এটা "সাপ্লাই" মানে সরবরাহ করার জন্য। *Core Java* বলে, এটা ডিফল্ট ভ্যালু জেনারেট করতে বা লেজি ইনিশিয়ালাইজেশনে ব্যবহার হয়।

**সহজ উদাহরণ:** র্যান্ডম টাস্ক জেনারেট করব।
```java
import java.util.function.Supplier;
import java.util.Random;

Supplier<String> taskGenerator = () -> "Task: Debug Vortex " + new Random().nextInt(100); // ল্যামডা
System.out.println(taskGenerator.get()); // আউটপুট: Task: Debug Vortex 42 (র্যান্ডম)
```
**কেন গুরুত্বপূর্ণ?** কনফিগারেশন বা টেস্ট ডেটা তৈরিতে দরকারি। স্প্রিং বুট-এ বিহেইভিয়ার সাপ্লাই করতে ব্যবহার হয়।

#### ৫. ফাঙ্কশন ল্যামডা (Function Lambda): ইনপুট নিয়ে আউটপুট রিটার্ন করে
**থিয়রি:** `Function<T, R>` ইন্টারফেসে `apply(T t)` মেথড আছে, যা `T` ইনপুট নিয়ে `R` রিটার্ন করে। এটা "ট্রান্সফর্ম" করার জন্য। *Core Java* বলে, স্ট্রিম API-তে ম্যাপিং-এ এটা ব্যবহার হয় (যেমন স্ট্রিংকে ইন্টে কনভার্ট)।

**সহজ উদাহরণ:** সংকে লেংথ ক্যালকুলেট করব।
```java
import java.util.function.Function;
import java.util.Arrays;
import java.util.List;

Function<String, Integer> lengthCalc = str -> str.length(); // ল্যামডা
List<String> names = Arrays.asList("Alice", "Bob");
names.stream().map(lengthCalc).forEach(System.out::println); // আউটপুট: 5 \n 3
```
**কেন গুরুত্বপূর্ণ?** ডেটা ট্রান্সফর্মেশনে সাহায্য করে। ই-কমার্স অ্যাপে প্রাইস ক্যালকুলেশনে ব্যবহার হয়।

#### সারাংশ এবং অতিরিক্ত টিপস
- **সব ল্যামডার কমন পয়েন্ট:** এগুলো `java.util.function` প্যাকেজে আছে। স্ট্রিম API (`stream().filter(predicate).map(function)`) এর সাথে কম্বাইন করুন।
- **অন্যান্য ল্যামডা টাইপ:** `BiPredicate` (দুটো ইনপুট), `BiConsumer` (দুটো ইনপুট ভয়েড), `BiFunction` (দুটো ইনপুট একটা আউটপুট) – এগুলো "Bi" দিয়ে শুরু হয়।
- **প্র্যাকটিস টিপ:** IDE-তে (যেমন IntelliJ) ল্যামডা লিখলে অটো-কমপ্লিট হয়। সতর্কতা: ল্যামডা ভেরিয়েবল ক্যাপচার করে, তাই ফাইনাল ভেরিয়েবল ব্যবহার করুন।
  এগুলো প্র্যাকটিস করুন কোড লিখে। যদি কোনো উদাহরণ বা কোড রান করে দেখাতে চান, বলুন! 😊